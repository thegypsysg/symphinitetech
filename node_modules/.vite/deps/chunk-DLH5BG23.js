import {
  useTextColor
} from "./chunk-BZZVPVNQ.js";
import {
  makeThemeProps,
  provideTheme
} from "./chunk-GELYZZFS.js";
import {
  convertToUnit,
  genericComponent,
  useRender
} from "./chunk-3JHAZOTH.js";
import {
  computed,
  createVNode,
  toRef
} from "./chunk-SE5EWVSA.js";

// node_modules/vuetify/lib/components/VDivider/VDivider.mjs
import "C:/Projects/symphinitetech/node_modules/vuetify/lib/components/VDivider/VDivider.css";
var VDivider = genericComponent()({
  name: "VDivider",
  props: {
    color: String,
    inset: Boolean,
    length: [Number, String],
    thickness: [Number, String],
    vertical: Boolean,
    ...makeThemeProps()
  },
  setup(props, _ref) {
    let {
      attrs
    } = _ref;
    const {
      themeClasses
    } = provideTheme(props);
    const {
      textColorClasses,
      textColorStyles
    } = useTextColor(toRef(props, "color"));
    const dividerStyles = computed(() => {
      const styles = {};
      if (props.length) {
        styles[props.vertical ? "maxHeight" : "maxWidth"] = convertToUnit(props.length);
      }
      if (props.thickness) {
        styles[props.vertical ? "borderRightWidth" : "borderTopWidth"] = convertToUnit(props.thickness);
      }
      return styles;
    });
    useRender(() => createVNode("hr", {
      "class": [{
        "v-divider": true,
        "v-divider--inset": props.inset,
        "v-divider--vertical": props.vertical
      }, themeClasses.value, textColorClasses.value],
      "style": [dividerStyles.value, textColorStyles.value],
      "aria-orientation": !attrs.role || attrs.role === "separator" ? props.vertical ? "vertical" : "horizontal" : void 0,
      "role": `${attrs.role || "separator"}`
    }, null));
    return {};
  }
});

export {
  VDivider
};
//# sourceMappingURL=chunk-DLH5BG23.js.map
